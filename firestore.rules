
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // GLOBAL BYPASS FOR PROTOTYPING
    // This allows all read/write operations to prevent "Missing or insufficient permissions"
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
