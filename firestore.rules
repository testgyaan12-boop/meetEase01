rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isSignedIn() && (
        request.auth.uid == 'hKv5CWVQv7YvJk8mLyCY11ec96O2' || 
        exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid))
      );
    }
    
    // MEETINGS
    match /meetings/{meetingId} {
      // Allow individual doc get if admin or owner
      allow get: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);
      // Allow listing if admin or if the query is filtered by the user's own ID
      allow list: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);
      
      allow create: if true;
      allow update: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);
      allow delete: if isAdmin();
    }
    
    // ADMIN NOTIFICATIONS - Anyone can create, only admins read
    match /admin_notifications/{notificationId} {
      allow create: if true;
      allow read: if isAdmin();
      allow update, delete: if isAdmin();
    }
    
    // AVAILABLE SLOTS - Public read
    match /availableSlots/{slotId} {
      allow read: if true;
      allow write: if isAdmin();
      allow list: if true;
    }
    
    // ROLES ADMIN - Authenticated users can read
    match /roles_admin/{adminUid} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }
    
    // USERS
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isSignedIn() && request.auth.uid == userId;
      allow delete: if isAdmin();
    }
  }
}